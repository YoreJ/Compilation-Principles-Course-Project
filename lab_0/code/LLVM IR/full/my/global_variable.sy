int global_var = 10;  // 全局变量

int main() {
    int x;
    x = getint();
    
    if (x > global_var) {
        global_var = x;  // 修改全局变量
    }
    
    putint(global_var);  // 输出全局变量的值
    putch(10);  // 输出换行符
    return 0;
}