int main() {
    int i;
    i = 0;
    
    while (i < 5) {  // 当 i 小于 5 时循环
        putint(i);  // 输出 i 的值
        putch(32);  // 输出空格
        i = i + 1;  // i 自增
    }
    
    putch(10);  // 输出换行符
    return 0;
}