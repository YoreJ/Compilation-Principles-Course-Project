int main() {
    int x;  // 定义一个整数变量
    int arr[5];  // 定义一个大小为5的整数数组
    
    x = 10;
    arr[0] = x;  // 将 x 的值赋给数组的第一个元素

    putint(arr[0]);  // 输出数组的第一个元素
    putch(10);  // 输出换行符
    
    return 0;
}